{
  "name": "monorepo-poc-yarn",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "package-delivery": "ts-node scripts/script-1.ts"
  },
  "devDependencies": {
    "turbo": "^1.2.6"
  },
  "dependencies": {
    "inquirer": "^8.2.4"
  }
}
